import { PACKAGE_NAME as PROTOCOL } from "@x32/protocol";

/**
 * Bridge entry point. Owns the `MixerClient` and serves the WebSocket API.
 *
 * Scaffolding placeholder — the WS server arrives in plan step 9, the X32
 * adapter (`src/x32/`, the only place that knows OSC) in step 10.
 */
function main(): void {
  console.log(`x32-bridge: scaffolding placeholder (wire protocol: ${PROTOCOL})`);
}

main();
