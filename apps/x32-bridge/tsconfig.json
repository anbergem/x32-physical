{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Runs on Node and may use its globals/builtins. (The only other place
    // allowed to is `@x32/installation`'s `/node` subpath — see its tsconfig.)
    "types": ["node"]
  },
  "include": ["src"]
}
