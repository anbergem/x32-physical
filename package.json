{
  "name": "x32-physical",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.23.0",
  "scripts": {
    "typecheck": "pnpm -r run typecheck",
    "test": "vitest run",
    "build": "pnpm --filter @x32/web run build",
    "dev": "pnpm --filter @x32/web run dev",
    "bridge": "pnpm --filter @x32/x32-bridge run dev"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
